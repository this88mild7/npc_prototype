<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neonex.npa.dao.LayoutDao">
    
    <select id="selectLayoutInfo" resultType="LayoutInfo">
        SELECT 
        	ELEMENT_UPPERCODE as elementUppercode,
			SEQ as seq,
			ACTIVE_YN as activeYn,
			PAGING as paging,
			ELEMENT_SECONDCODE as elementSecondcode
        FROM TVO_LAYOUT_SET_INFO
    </select>
    
</mapper>